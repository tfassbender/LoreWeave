# Overrides that only apply when the 'dev' profile is active (i.e. ./gradlew quarkusDev).
# Tests and the packaged prod fast-jar use application.properties defaults instead.

# Verbose logging during development — both console and the rotating file capture DEBUG.
quarkus.log.level=DEBUG
quarkus.log.file.level=DEBUG
